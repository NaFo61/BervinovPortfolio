**Badge** — outlined pill label, never filled. `mint` flags featured/popular items; `success`/`info` for status; `neutral` for plain tags.

```jsx
<Badge variant="mint">Популярный</Badge>
<Badge variant="neutral">HoReCa</Badge>
```
