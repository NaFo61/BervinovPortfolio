**Input** — dark form field with a label. The one sharp (5px) radius in the system; focus turns the border Deep Signal blue. Use `as="textarea"` for messages.

```jsx
<Input label="Имя" placeholder="Как вас зовут" />
<Input label="Сообщение" as="textarea" rows={4} />
```
