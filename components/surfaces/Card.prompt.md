**Card** — primary container. Elevation is the light hairline border, never a shadow. Use `featured` for the single mint-glow card per viewport, `interactive` for hover lift on clickable cards.

```jsx
<Card>…</Card>
<Card featured>BervinovAcademy — флагман</Card>
<Card interactive surface="raised">…</Card>
```
