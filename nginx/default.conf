map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream showcase_api {
    server api:8000;
    keepalive 8;
}

# --- Health (для docker / host checks) ---
server {
    listen 80;
    server_name _;

    location = /health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 'ok';
    }

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- Портфолио ---
server {
    listen 80;
    server_name bervinov-miron.ru www.bervinov-miron.ru localhost;

    root /srv/sites/portfolio;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- Северный Кофе ---
server {
    listen 80;
    server_name severny-kofe.bervinov-miron.ru;

    root /srv/sites/severny-kofe;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- Vкус&Огонь ---
server {
    listen 80;
    server_name vkus-ogon.bervinov-miron.ru;

    root /srv/sites/vkus-ogon;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- ДентаЛюкс ---
server {
    listen 80;
    server_name denta-lux.bervinov-miron.ru;

    root /srv/sites/denta-lux;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- FitZone ---
server {
    listen 80;
    server_name fitzone.bervinov-miron.ru;

    root /srv/sites/fitzone;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- LinguaPro ---
server {
    listen 80;
    server_name lingua-pro.bervinov-miron.ru;

    root /srv/sites/lingua-pro;
    index index.html;

    location /api/ {
        proxy_pass http://showcase_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
